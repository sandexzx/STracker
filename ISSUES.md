# STracker — Список проблем и недоработок

*Документ создан: 25 января 2026*

---

## 🔴 Критические ошибки (Crashes)

### 1. Вылет при нажатии на "+" в библиотеке упражнений
**Файл:** [STrackerNavHost.kt](app/src/main/java/com/example/stracker/presentation/navigation/STrackerNavHost.kt)

**Проблема:** При попытке создать новое упражнение приложение падает с ошибкой:
```
Navigation destination that matches request NavDeepLinkRequest{ uri=android-app://androidx.navigation/exercises/create } cannot be found
```

**Причина:** В `Screen.kt` объявлен маршрут `CreateExercise`, но в `STrackerNavHost.kt` отсутствует соответствующий `composable()` для этого маршрута.

**Решение:** 
- Создать `CreateExerciseScreen.kt` и `CreateExerciseViewModel.kt`
- Добавить `composable(Screen.CreateExercise.route)` в NavHost

---

### 2. Вылет при выборе тренировки в истории
**Файл:** [STrackerNavHost.kt](app/src/main/java/com/example/stracker/presentation/navigation/STrackerNavHost.kt)

**Проблема:** При клике на тренировку в истории — crash:
```
Navigation destination that matches request NavDeepLinkRequest{ uri=android-app://androidx.navigation/workout/2 } cannot be found
```

**Причина:** В `Screen.kt` объявлен маршрут `WorkoutDetail`, но в `STrackerNavHost.kt` отсутствует `composable()` для него.

**Решение:** 
- Создать `WorkoutDetailScreen.kt` и `WorkoutDetailViewModel.kt` (или заменить на показ деталей в диалоге)
- Добавить `composable(Screen.WorkoutDetail.route, arguments = ...)` в NavHost

---

## 🟠 Проблемы навигации

### 3. Нет перехода между вкладками "История" и "Упражнения"
**Файл:** [WorkoutHistoryScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/history/WorkoutHistoryScreen.kt#L128-L133)

**Проблема:** С вкладок "История" и "Упражнения" нельзя переключаться между собой. Нижняя навигация не работает полноценно — можно вернуться только на Home.

**Причина:** В `BottomNavBar` на экране `WorkoutHistoryScreen` обработчик `BottomNavItem.EXERCISES` пустой (`{ /* Navigate */ }`), нет callback для перехода на Exercises.

**Решение:** 
- Добавить в `WorkoutHistoryScreen` и `ExerciseLibraryScreen` параметры для навигации между всеми вкладками
- Реализовать переходы через `navController` или callback

---

### 4. Кнопка "Настройки" не кликабельна
**Файл:** [HomeScreen.kt](app/src/main/java/com/example/stracker/presentation/home/HomeScreen.kt#L61)

**Проблема:** В хедере HomeScreen есть `Pill(text = "⚙️ Настройки")`, но он не обрабатывает клики.

**Причина:** Компонент `Pill` не имеет параметра `onClick` или он не реализован.

**Решение:**
- Добавить в `Pill` поддержку клика
- Создать `SettingsScreen` и реализовать переход на него
- ИЛИ удалить кнопку настроек, если она не нужна на MVP

---

## 🟡 Отсутствующие экраны (согласно спецификации)

### 5. Экран создания упражнения (CreateExerciseScreen)
**Статус:** ❌ Не реализован

**По спецификации:** Пользователь должен иметь возможность создать своё упражнение с указанием названия, категории, мышечной группы.

**Необходимо создать:**
- `presentation/exercise/create/CreateExerciseScreen.kt`
- `presentation/exercise/create/CreateExerciseViewModel.kt`
- Добавить маршрут в NavHost

---

### 6. Экран деталей тренировки (WorkoutDetailScreen)
**Статус:** ❌ Не реализован

**По спецификации:** При клике на тренировку в истории должен открываться экран с деталями всех упражнений и подходов.

**Необходимо создать:**
- `presentation/workout/detail/WorkoutDetailScreen.kt`
- `presentation/workout/detail/WorkoutDetailViewModel.kt`
- Добавить маршрут в NavHost

---

### 7. Экран настроек (SettingsScreen)
**Статус:** ❌ Не реализован

**По спецификации:** Настройки включают:
- Формула e1RM (Epley/Brzycki)
- Единицы веса (kg/lbs)
- Шаг веса по умолчанию
- Показывать ли RPE
- Тема (Light/Dark/System)

**Необходимо создать:**
- `presentation/settings/SettingsScreen.kt`
- `presentation/settings/SettingsViewModel.kt`
- DataStore для хранения настроек
- Добавить маршрут в NavHost

---

### 8. Экран статистики (StatsScreen)
**Статус:** ❌ Не реализован (указан в спецификации)

**По спецификации:** Общая статистика и аналитика.

---

## 🟡 Проблемы UX и функциональности

### 9. Добавленное упражнение не появляется сразу
**Файл:** [ActiveWorkoutViewModel.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutViewModel.kt)

**Проблема:** После добавления упражнения в тренировку оно не отображается сразу. Только после перезапуска приложения.

**Причина:** ViewModel подписывается на `Flow` из Room, но возможно проблема в том, что:
1. После `addExerciseToWorkout` не происходит эмит в Flow
2. Или используется `.first()` вместо `.collect()`

**Решение:** Проверить, что `WorkoutExerciseDao` эмитит изменения, или вручную перезагрузить данные после добавления.

---

### 10. Добавленный подход не появляется сразу
**Файл:** [ActiveWorkoutViewModel.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutViewModel.kt#L93-L100)

**Проблема:** После добавления подхода он не отображается в списке сразу.

**Причина:** Аналогично п.9 — Flow может не эмитить изменения или UI не обновляется.

**Решение:** Убедиться, что Room Flow реактивен. Возможно, нужно использовать `workoutDao.getWorkoutByIdFlow()` вместо синхронных методов.

---

### 11. Поле веса показывает "0.0" как значение, а не placeholder
**Файл:** [ActiveWorkoutScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutScreen.kt#L402-L410)

**Проблема:** При добавлении подхода в поле веса стоит "0.0", которое нужно стереть вручную.

**Текущий код:**
```kotlin
var weight by remember { 
    mutableStateOf(
        (advice?.recommendedWeight ?: lastSet?.weight ?: 0f).toString()
    ) 
}
```

**Решение:** 
- Использовать пустую строку, если вес 0
- Показать "0.0" как placeholder, а не как value
```kotlin
var weight by remember { 
    mutableStateOf(
        (advice?.recommendedWeight ?: lastSet?.weight)?.takeIf { it > 0 }?.toString() ?: ""
    )
}
// И добавить placeholder в TextField
```

---

### 12. Вес предыдущего подхода не подставляется автоматически
**Файл:** [ActiveWorkoutScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutScreen.kt#L400-L415)

**Проблема:** При добавлении следующего подхода хотелось бы видеть вес из предыдущего подхода. Сейчас рекомендация может быть, но если её нет — подставляется 0.

**Решение:**
```kotlin
val defaultWeight = advice?.recommendedWeight 
    ?: lastSet?.weight 
    ?: workoutExercise.sets.lastOrNull()?.weight 
    ?: 0f
```
Убедиться, что `lastSet` берётся из текущей тренировки.

---

### 13. Количество повторений не подставляется из предыдущего подхода
**Файл:** [ActiveWorkoutScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutScreen.kt#L405-L410)

**Проблема:** Аналогично весу — хочется, чтобы повторения подставлялись из предыдущего подхода.

**Решение:**
```kotlin
val defaultReps = advice?.recommendedReps 
    ?: lastSet?.reps 
    ?: workoutExercise.sets.lastOrNull()?.reps 
    ?: 8
```

---

### 14. Нет кнопок +/- для быстрой корректировки веса и повторений
**Файл:** [ActiveWorkoutScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutScreen.kt#L390-L460)

**Проблема:** Для удобства нужны кнопки:
- Для веса: +0.5 / -0.5 кг (или +2.5 / -2.5 для базовых)
- Для повторений: +1 / -1

**Решение:** Добавить рядом с полями ввода кнопки-стрелки или степперы:
```kotlin
Row {
    IconButton(onClick = { weight = (weight.toFloat() - 0.5f).toString() }) {
        Icon(Icons.Default.Remove, "Уменьшить")
    }
    OutlinedTextField(...)
    IconButton(onClick = { weight = (weight.toFloat() + 0.5f).toString() }) {
        Icon(Icons.Default.Add, "Увеличить")
    }
}
```

---

### 15. Нет объяснения, что такое RPE
**Файлы:** 
- [ActiveWorkoutScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/active/ActiveWorkoutScreen.kt#L423-L430)
- [HomeScreen.kt](app/src/main/java/com/example/stracker/presentation/home/HomeScreen.kt#L130-L135)

**Проблема:** RPE упоминается, но нигде не объясняется, что это такое.

**Решение:**
- Добавить иконку "?" или "ⓘ" рядом с полем RPE
- При клике показывать tooltip/dialog с объяснением:
  > **RPE (Rate of Perceived Exertion)** — субъективная оценка усилия от 1 до 10.
  > - 6-7: Могу сделать ещё 3-4 повтора
  > - 8: Могу сделать ещё 2 повтора
  > - 9: Могу сделать ещё 1 повтор
  > - 10: Максимальное усилие, больше не могу

---

### 16. Фильтр в истории не работает
**Файл:** [WorkoutHistoryScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/history/WorkoutHistoryScreen.kt#L62)

**Проблема:** Кнопка "Фильтр" в WorkoutHistoryScreen ничего не делает.

**Решение:** 
- Реализовать фильтрацию по дате/неделе/месяцу
- Или удалить кнопку до реализации

---

## 🔵 Улучшения компонентов

### 17. Pill компонент без поддержки клика
**Файл:** Нужно найти в `presentation/common/components/`

**Проблема:** `Pill` используется для кнопок ("Настройки", "Фильтр"), но не поддерживает onClick.

**Решение:** Добавить опциональный `onClick: (() -> Unit)? = null` параметр.

---

### 18. BottomNavBar не полностью функционален на всех экранах
**Файлы:**
- [WorkoutHistoryScreen.kt](app/src/main/java/com/example/stracker/presentation/workout/history/WorkoutHistoryScreen.kt#L123-L133)
- [ExerciseLibraryScreen.kt](app/src/main/java/com/example/stracker/presentation/exercise/library/ExerciseLibraryScreen.kt) — нет BottomNavBar вообще

**Проблема:** 
- В WorkoutHistoryScreen навигация на Exercises не работает
- В ExerciseLibraryScreen BottomNavBar отсутствует

**Решение:** Добавить полноценную навигацию на всех экранах или сделать единый Scaffold с BottomNavBar на уровне MainActivity.

---

## 🟢 Незначительные улучшения

### 19. Отсутствует поддержка разметки разминочных подходов
**По спецификации:** Подходы могут быть помечены как `isWarmup: Boolean`.

**Проблема:** В UI нет возможности отметить подход как разминочный.

---

### 20. Нет таймера отдыха между подходами
**По спецификации:** Опциональный таймер отдыха с вибрацией (backlog).

---

### 21. Нет валидации ввода
**Проблема:** В полях ввода нет проверки на:
- Отрицательные значения
- Слишком большие значения
- Пустые поля

---

### 22. Кнопки фильтра мышечных групп не покрывают все группы
**Файлы:**
- [ExerciseLibraryScreen.kt](app/src/main/java/com/example/stracker/presentation/exercise/library/ExerciseLibraryScreen.kt#L137-L146)
- [ExercisePickerScreen.kt](app/src/main/java/com/example/stracker/presentation/exercise/picker/ExercisePickerScreen.kt#L127-L138)

**Проблема:** Показываются только: Грудь, Спина, Ноги, Плечи, Руки. Нет: Трицепс, Пресс, Икры и др.

---

### 23. Нет индикатора загрузки на некоторых экранах
**Проблема:** При загрузке данных нет визуальной индикации.

---

### 24. Нет обработки ошибок в UI
**Проблема:** Состояния `error` есть в State, но они не отображаются пользователю.

---

## 📋 Приоритезированный план исправлений

### Высокий приоритет (блокирующие баги):
1. ❌ Исправить навигацию на CreateExercise (создать экран)
2. ❌ Исправить навигацию на WorkoutDetail (создать экран)
3. ❌ Исправить обновление списка упражнений/подходов в реальном времени
4. ❌ Добавить навигацию между History и Exercises

### Средний приоритет (UX):
5. ⚠️ Автозаполнение веса/повторений из предыдущего подхода
6. ⚠️ Кнопки +/- для корректировки значений
7. ⚠️ Tooltip для объяснения RPE
8. ⚠️ Placeholder вместо "0.0" для пустых полей

### Низкий приоритет (polish):
9. 📝 Экран настроек
10. 📝 Фильтр в истории
11. 📝 Разминочные подходы
12. 📝 Таймер отдыха

---

## Статистика

| Категория | Количество |
|-----------|------------|
| 🔴 Критические (crashes) | 2 |
| 🟠 Навигация | 2 |
| 🟡 Отсутствующие экраны | 4 |
| 🟡 UX проблемы | 8 |
| 🔵 Улучшения компонентов | 2 |
| 🟢 Незначительные | 6 |
| **Итого** | **24** |
